[package]
name = "nle-decode"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Supervised FFmpeg monitor-frame decoding for Maelstrom."

[features]
test-hooks = []

[dependencies]
ffmpeg-next.workspace = true
nle-cache = { path = "../nle-cache" }
tracing.workspace = true
