# Blick Foundation Requirement Audit

Audit date: 2026-08-24. Authority: `RUST_NLE_FOUNDATION_PLAN.md` v1.1 plus the
user-directed splash, Project Hub, bilingual UI, editor, persistence, and packaging behavior.

`Proven` means current source plus a directly covering automated or packaging check supports the
claim. `Partial` means implementation exists but the full exit criterion still needs stronger
runtime evidence. Windows is the supported product target; Linux is an optional later milestone
and macOS is explicitly out of scope.

## Acceptance and performance

| Requirement | Status | Current evidence |
|---|---|---|
| Native desktop binary; no web/Electron | Proven | `nle-app` owns `winit`/`wgpu`; Windows package smoke starts the native executable. |
| First native surface presents in <1 s | Proven on current Windows package | A one-shot worker report starts at `App` construction and records only after the first successful `wgpu` surface presentation. Packaging rejects ≥1,000 ms; the current proof is 608.705 ms, with recent complete passes remaining at 608.705–775.232 ms. The original branding art remains intact, but the native window embeds a purpose-sized 256 px icon. Project Hub backdrops reuse the splash renderer's decoded RGBA instead of decoding both 1,672×941 PNGs again, and their egui installation, hardware detection, catalog/thumbnail IO, manifest-driven model preloading, and native audio-device negotiation all begin only after the first present. |
| Blank bars immediately; file dialog and OS/timeline drop | Proven | Media Pool and native multi-file drop paths place bars immediately and emit analysis only afterward. Drops use non-ripple overwrite semantics on occupied V1/A1 time, preserving source-accurate outer tails, links, later positions, analysis dispatch, and exact undo; the explicit Add action remains append-only. Unprobed bars durably record exact provisional ownership: analysis may extend untouched placeholders to the real duration but cannot rewrite razor, trim, slip, replace, or overwrite decisions. The release-package gate waits for real Media Pool and timeline layout geometry, claims the visible card through the production drag path, and requires the linked drop before downstream media acceptance can pass. |
| 50k bars remain O(visible) with banding | Proven | `banding_reduces_draw_count_when_zoomed_out`, randomized visibility test, native primitive bound, release harness. |
| Input/timeline path ≤2 ms | Proven for CPU path | Current release evidence: playhead p95 0.123 ms; wide/detail 50k paths remain below budget, and the move/trim, inverse-history, far-relocation, and binary-search gates reject ≥2 ms. |
| Defining combined demo: real H.264 scrub with 20k+ bars | Proven on supplied Windows media | The current release integration run kept 20,002 bars in the retained editor canvas while alternating real H.264 requests from 12 s back to 3 s. It emitted 1,343 native primitives, measured complete playhead mutation plus visual frame at 0.224 ms p95, and delivered the coalesced latest 3 s software-decoded RGBA frame. |
| Full CPU UI frame p95 ≤8 ms | Proven on current Windows editor | Current release canvas evidence: wide p95 0.305 ms and detail p95 0.115 ms; the packaged real-media gate independently measures the complete production frame. |
| GPU execution and ≥60 surface submissions/s | Proven on current Windows package; compositor scanout timing is not measured | Real RTX 3090 offscreen execution currently validates 1,000 banded bars and both native shaders at 0.198 ms p95. The project-built-FFmpeg package gates 120 CPU frame/surface-submit intervals in a visible FIFO window; the current pass measured 158.13 submissions/s average, 6.949 ms interval p95, and 1.149 ms CPU p95. Recent full-package repetitions remained at 80.56–158.24 submissions/s, 6.461–18.721 ms interval p95, and 0.954–1.662 ms CPU p95. The smoke starts after a fixed two-second post-build/fixture quiet interval while retaining the same sample count and rejection thresholds, preventing transient compiler/linker pressure from masquerading as renderer work. The bounded probe stays above other windows only while collecting those frames, then restores normal window level; hidden/occluded Windows surfaces are not used because the OS independently throttles their redraw delivery. These are submission measurements, not display scanout or GPU-completion timestamps. |
| Async monitor never stalls timeline | Proven structurally and on supplied media | UI hot path has no FFmpeg/IO. The scheduler coalesces before a seek, completes one progressive frame instead of starving on reverse arrivals during preroll, and passes forward and continuous reverse request-storm tests against the project-built FFmpeg runtime. |
| Export starts/cancels while editor remains live | Proven | Snapshot-at-start export worker and real H.264/AAC tests; the latest packaged real-media smoke confirms FFmpeg progress, advances playback by 1.462 seconds while export is active, cancels it, rejects partial output, and terminates the exact process tree during cleanup. |
| Background atomic autosave | Proven | Per-project coalescing writer, non-blocking Hub navigation, retained failure retry, backup recovery, final flush/retry, project reopen, moved-relative-media, and forced-process-termination recovery tests. An app-level analysis regression resolves 15→60 seconds without a video strip, drains the asynchronous writer, closes and reopens the project, and proves both the existing 60-second bars and a future 60-second placement. |

The plan’s automatic blank-project transition is intentionally superseded by the user-required
click-to-continue splash and Project Hub. Its responsiveness target remains enforced: the first
surface presents in under one second, then startup resource work—including model preloading—remains
behind the splash. No media analysis or model file IO runs on the UI thread.

## User-directed product behavior

The Project Hub request described DaVinci-style controls as a feature pool rather than a demand to
copy every control. This table distinguishes those selected features from hard follow-up fixes.

| User-directed behavior | Status | Direct evidence |
|---|---|---|
| English and Japanese art forms a slowly rotating, tilted, open cylinder over true black | Proven | The embedded PNGs feed `SplashRenderer`; its native depth-tested mesh has separate inner/outer faces, mirrored inner UVs, fixed tilt, time-based rotation, and black clear color. `cylinder_mesh_has_valid_indices_and_unit_uvs` covers the mesh. |
| Both cylinder openings retain equal empty gaps instead of joining | Proven | Both panel arcs derive from the same `PANEL_ARC` and `PANEL_GAP`; `cylinder_panels_have_equal_openings` measures both openings independently and rejects a seam. |
| Splash requires a click and hides startup work | Proven | `splash_never_allows_continuing_before_resources_are_ready` and `splash_only_allows_continuing_after_the_minimum_presentation` cover the gate. Hardware, catalog/thumbnail, models, and audio-device initialization start after first present and must finish before continuation activates. |
| Detect discrete GPU, Intel/Quick Sync, and useful Windows media backends during splash | Proven | Hybrid-GPU, duplicate-collapse, software-only fallback, actual `wgpu` enumeration, and FFmpeg backend tests cover renderer/media recommendations. The inspector reports the decoder that actually produced frames. |
| Preload multiple declared models behind splash | Proven infrastructure | A bounded versioned manifest preloads validated model bytes into a retained registry on the startup worker. Tests cover multiple stable IDs plus duplicate, unsafe, missing, and size-mismatched entries. The shipped manifest is intentionally empty until product models are selected. |
| Project Hub stays a smaller decorated window; editor maximizes without hiding the taskbar | Proven | Hub removes fullscreen, restores native decorations, unmaximizes, and requests 1240×780; editor removes fullscreen, keeps decorations, and maximizes. This preserves native window controls and the Windows taskbar. |
| Dark UI, bilingual selector, language-specific translucent backdrop | Proven | Hub/editor use the dark palette and embedded Noto Sans JP. `HubBackdrops::selected` switches 20/255-alpha art immediately, and `action_carries_language` proves the choice reaches project opening and the editor. Light mode remains explicitly later work. |
| New/Open/Templates, search, sorting, list/grid, distributed Name/Last opened/Size columns, thumbnail scale, tooltips | Proven | Production Hub controls implement each item; `search_is_case_insensitive`, `sorts_work`, `thumbnail_clamps`, and `list_columns_fill_the_available_width` cover state and geometry. Type, cloud, and tutorial clutter are absent. |
| Project import/export, duplicate/copy, collections control, persistent cards and thumbnails | Proven for selected Hub scope | Project import/export and duplicate run on background workers; catalog writes are asynchronous and durable. Collections can be created in the Hub session. Project paths, sizes, cards, and representative thumbnails persist; reopen/thumbnail/catalog tests cover durable behavior. |
| Deduplicated 16:9 and 9:16 templates with aspect graphics and combined social targets | Proven | Eight presets include 720p, 1080p, combined 2160p/4K, 8K, vertical 720p, combined 1080p Shorts/TikTok/Instagram/Facebook, vertical 4K, and audio. Template tests reject duplicate parameter sets and verify combined labels. |
| Enter in the project-name field behaves like Create | Proven structurally | The focused name edit submits `NewProject` on Enter through the same branch as the Create button, carrying template and language. |
| Supplied logo and icon are used | Proven | Source artwork is retained under `assets/branding`; the embedded purpose-sized icon is validated and used by the native Windows window and package executable. |
| Editor defaults to a smaller viewer and larger timeline; playback controls centered; Analysis does not overlap | Proven | Responsive default is 66% timeline and package geometry rejects a viewer-heavy layout. Transport width is centered from the viewer’s live available width; Analysis is a separate resizable panel. Layout tests cover the default across window sizes. |
| Draggable frames resize editor panels and individual timeline tracks | Proven | Horizontal/vertical splitters persist workspace geometry. Full-width row boundaries use vertical-resize cursors and durable per-track heights; a production-canvas test exercises the gesture. |
| Media imports by dialog/drop, shows a thumbnail, supports timeline drag and right-click Add to Timeline | Proven | The complete Media Pool card is one native drag source and owns a bilingual context menu. Production pointer tests cover empty/occupied drops, thumbnail-edge starts, scroll contention, cancellation, and linked overwrite; package acceptance uses real rendered geometry. |
| First empty-timeline placement begins at 00:00 and clips can move afterward | Proven | Zero-start, external-hover, pointer-move, linked-move, and exact undo/redo tests cover placement and relocation. |
| Three video and three audio tracks exist initially; context menu can add tracks | Proven | Kernel/editor default-layout tests cover six initial tracks. The ruler/track context menu and command palette add either kind. |
| Video thumbnails and normalized audio waveform become visible as rows expand | Proven | FFmpeg workers create bounded strip atlases and peak arrays only after placement; native texture/primitive tests cover rendering. Quiet non-silent sources normalize visually without changing gain; silence stays flat. |
| Centered audio gain line is aligned, draggable, persisted, and changes live output | Proven | Drawing and hit testing share `audio_gain_y`; production pointer tests drag 0 dB to -12 dB, update live playback every pointer sample, and round-trip the value. The native-device package gate measures real attenuation without restarting decode. |
| Independent hover fades exist on every razor segment, shape is adjustable, video reaches black and audio silence | Proven | Razor creates independent sections with independent envelopes. Production hit/drag tests cover zero-duration hover handles, overlap priority, length, and curve. Viewer alpha proves black; audio tests and package meters prove silence/attenuation. |
| Red playhead attachment scrubs continuously both directions with sharp live preview | Proven | The wide ruler handle owns the gesture across pointer frames. Supplied-media forward/backward/reverse-storm and combined 20k-bar gates prove live intermediate frames and latest-target accuracy. No `Preview pending` UI remains; the last valid monitor frame is retained while decode catches up. |
| Real audible playback, live meters, useful all-stream metadata | Proven on current Windows device/media | Native CPAL produces non-silent PCM, keeps A/V within one 24 fps frame, and mixes every active lane. Meters read live output; FFmpeg probe reports every stream’s codec, dimensions/channels, rates, timing, duration, time base, and bit rate. |
| Adobe-style two-handle zoom and Blick/DaVinci editing tools | Proven | Two draggable circular handles control view span. Pointer, range, trim/roll, slip, razor, snapping, linking, position lock, overwrite/insert/replace, markers/flags, mute, undo/redo, palette, wheel zoom, and pan have kernel or production-input tests. |
| Autosave/reopen preserve media, clips, timing, gain/fades, layout, card size, and thumbnail | Proven | Versioned snapshots exclude only runtime caches/transport. App-level reopen tests preserve media and linked clips; catalog and thumbnail writers are coalesced, atomic, and crash-recoverable. |
| Stock LGPL FFmpeg powers probe, decode, audio, strips/waveforms, and export with adjacent DLLs | Proven in Windows package | Scrub/playback link shared libraries in-process; cancellable helpers handle analysis/export. Packaging verifies the pinned bundle, licenses, hashes, DLL closure, adjacent-only startup, real media, cancellation, and process cleanup. |
| Tests release the computer after every run | Proven operationally | Package/app helpers have bounded exits and exact recursive cleanup checks. Hands-on runs close Maelstrom; final handoff checks Maelstrom, FFmpeg, and FFprobe processes. |

## Phase evidence

| Phase | Status | Evidence or remaining proof |
|---|---|---|
| 0 — window/chrome/navigation/HUD | Proven on current Windows package | Native first surface in 608.705 ms on the current package. Hub-only texture installation, catalog/thumbnail IO, model preloading, hardware detection, and audio negotiation start afterward. Model-loader tests retain multiple files by stable ID and reject unsafe, duplicate, missing, and size-mismatched entries independently; Windows packaging validates and copies the manifest plus every declared file. Native split editor; functional bilingual File/Edit/View/Help menus; empty ruler/tracks; ruler scrub; wheel zoom; Shift-wheel/MMB/Alt pan; CPU HUD; tracing; debug VSync; real GPU execution and an honestly labeled 120-frame packaged surface-submission gate. |
| 1 — kernel/50k/history/commands | Proven | Sorted insert, `clip_at_playhead`, randomized visible range, razor continuity, non-inverting trim, capped inverse undo, banding, release 50k gates; command add-track/razor/delete. Same-cardinality history capture compares stable IDs without allocating full-project maps, stores only the changed clip for a single relocation, and falls back for structural edits. The editor records against the live timeline instead of cloning a redundant after snapshot; a far relocation across 50,000 clips completes its edit/release history path in 1.21–1.52 ms over five release runs and round-trips exact undo/redo. |
| 2 — import/probe/offline | Proven | 200 imports queue no analysis; placed media probes every stream and immediately show provisional bars. Worker-produced duration is a durable scalar rather than a display cache. Exact provisional clip IDs survive restore, extend or shorten atomically when analysis arrives, stop before the next occupied bar, and are abandoned by user source-timing edits. Their before/after ownership shares the capped timeline undo transaction: placement, delete, razor, and overwrite regressions restore the correct rights, while redo after analysis immediately uses the durable real duration instead of resurrecting the placeholder. Legacy 15-second placeholders migrate without treating newly saved intentional 15-second sections as unresolved. Kernel and editor regressions cover 15→60 extension, shortening with source offsets, linked A/V, overlap capping, razor preservation, first-view refit, restore, future placement from known duration, and pending/resolved undo ordering. Media Pool and Windows file drops perform non-ripple overwrite placement on occupied time while the Add action appends. Production-layout and edit-kernel regressions prove linked A/V tails, source offsets, fixed later positions, analysis emission, invariants, and exact undo. Deterministic media-ID hashing selects one of eight dark source palettes while preserving blue-family video and green-family audio identity; magenta remains exclusive to the editable offline state. |
| 3 — sticky/coalesced/cancellable decode | Proven on supplied H.264 media for Windows | Forward/backward/far/reverse-storm accuracy, live intermediate publication, latest request, sticky reuse, cancellation/drop, bounded cache, and hardware-to-software runtime fallback. The latest combined release gate alternated real H.264 requests from 12 s back to 3 s while the actual editor painted 20,002 bars, held input-to-visual p95 to 0.256 ms, and delivered the latest 3 s RGBA frame. Windows prefers FFmpeg's native D3D11VA/DXVA2 device context before named CUVID/Quick Sync fallbacks; on the supplied H.264 clip this reduced the gated 12s-to-3s backward seek from about 3.4 seconds to 186 ms. Reverse arrivals that remain ahead of the frame currently being decoded retarget the open GOP preroll instead of finishing obsolete work; the continuous reverse-storm gate passes repeatedly. Forced supplied-media tests exercise both generic Windows device backends, opaque-surface CPU transfer, and RGBA scaling in addition to named-decoder selection. |
| 4 — audio/waveforms/playback | Proven on current Windows device | CPAL output opens; supplied media yields non-silent PCM; coalesced 100-seek, backward-seek, rate-conversion, two-lane mix, shaped-fade, and live-meter tests pass. Gain and fade settings are applied in the live mixer rather than baked into queued PCM; deterministic tests change both over already-queued samples. The packaged native-device smoke applies a long fade and requires meter attenuation, clears it and requires audible recovery, then lowers gain and requires attenuation again—all without restarting decode. A viewer render-command test proves the video image mesh reaches alpha 0 over the black monitor background at the fade edge and returns to alpha 255 after clearing the fade. Production-canvas pointer tests press the centered 0 dB line on an expanded audio row and drag it to -12 dB, then prove the playback target and restored project retain that exact gain; every pointer sample requests a live mixer/display update. The gain rubber band is now a full-width retained timeline primitive emitted after the waveform, using the same y geometry as hit-testing so native compositing cannot hide or offset the actual control. The same gesture suite creates and shapes independent video/audio fades and proves the live playback targets receive them. Visible clip controls retain pointer ownership when a zoomed-out curve point overlaps trim-edge slop or an audio fade handle overlaps the neighboring track-resize grip. The timeline also shows a bounded live dB readout while the gain line is hovered or dragged. Every active unmuted track is sample-aligned into a bounded shared mix. The native callback owns the A/V clock, the video playhead maps to it, underruns advance as device silence while late lanes discard elapsed PCM to catch up, and a real-device 500 ms run stays within one 24 fps frame. |
| 5 — project IO/autosave | Proven on Windows | Versioned documents, relative/absolute paths, atomic backup recovery, durable-generation autosave and reopen tests pass. Analysis completion itself schedules durable duration state even when no video strip exists; a 15→60 second app-level regression drains the writer, closes/reopens, and proves the restored duration sizes both existing and future placements. Portable media tables may be harmlessly reordered: path pairs resolve first, restore canonicalizes them into compact ID order, and playback resolves both clips to the correct files. Sparse IDs are rejected before partial editor construction. Pending saves coalesce independently per project, failed requests remain queued behind newer state, and Return to Hub does not wait for disk. Successful background saves report their actual persisted byte count to the owner thread; the Hub and catalog update without UI-thread filesystem work, known-project opens refresh the value, and startup repairs legacy `0 B` entries in its resource worker. Native package verification shows the existing project as `4.0 KB`. Project catalog mutations are coalesced on a separate writer instead of touching the filesystem from Hub actions. The crash test starts the real background writer in a child process, waits for its latest edit to become durable, terminates it without `Drop`, and successfully reopens that edit. |
| 6 — real export | Proven | Immutable snapshot, gaps/trims/gain/fades, hardware preference/OpenH264 fallback, real H.264/AAC probe, cancel/no partial. |
| 7 — foundation polish | Proven on Windows | Hold-C razor, linked A/V, rational project-rate timecode/frame stepping/trim/monitor cadence, snapping, searchable media, range/slip/roll/pointer/razor tools, bilingual UI. New/reset workspaces give the timeline a responsive 66% of available editor height, keeping it at least 1.5× the viewer from compact laptop windows through 4K. The package acceptance report records production-frame panel geometry and rejects a viewer-heavy default; the latest 2560×1400 run measured an 875.82 px timeline over a 444.18 px viewer workspace. First placement replaces the empty 4:12 range with a fitted provisional view; analysis then atomically resolves untouched bars and refits without overriding user navigation or timing edits. Package acceptance records post-probe extents, requires its deterministic 60-second source to resolve within 59–61 seconds, and rejects an unfitted view; the latest run proved 63,000,000 visible ticks for 60,000,000 ticks of content, 2,048 waveform peaks, and 1,462,000 ticks of live playback. A foreground supplied-media capture visibly showed full-width thumbnails and waveform. A hands-on packaged pass opened the persistent project, confirmed Analysis no longer overlaps the viewer, dragged the visible playhead handle backward from 15:02 to 8:21, and observed an immediate sharp monitor frame; the app was then closed without changing the saved custom layout. A durable default/custom marker lets the responsive split follow later window sizes, while a user-dragged split persists exactly and survives temporary viewport clamping. Pre-marker project JSON migrates former exact 330 px, 520 px, 640 px, and 760 px defaults while preserving non-default custom heights; marker-era projects preserve every explicit custom height. The Analysis panel distinguishes no selection, imported-but-unplaced, active analysis, ready/live, and offline media in both languages; a fresh native package visibly reports `No selection` and `Select media` instead of a false busy state. A production-UI pointer regression drags a full-width track boundary from the timeline canvas while a clip is present, verifies only the intended row grows, releases ownership cleanly, and restores the exact height from the project snapshot. Screen transitions release stale text focus, and native Ctrl/Cmd undo, redo, razor, delete, and command-palette shortcuts bypass stale widget routing; packaged Windows interaction verified undo/redo after opening from a focused Project Hub search field. Linux is an optional later milestone; macOS is unsupported and out of scope. |

## Must-employ checklist

| Item | Status | Evidence |
|---|---|---|
| Bars/SoA/binary search/banding; zero FFmpeg on timeline path | Proven | `nle-timeline`, retained primitive buffers, 50k gates, dependency boundary. Clip labels and the two waveform-status messages are retained galleys, clipped to their own bars, reused across frames, invalidated on DPI changes, and omitted below readable width. Waveform, thumbnail, failure/offline, and flag state is resolved into a compact retained media-slot array only when its source changes; the visible-clip loop performs one array lookup and no media `HashMap` or flag scan. Flags use a fixed two-rectangle overlay rather than allocating polygon vertices, preserving their layer above native thumbnail tiles. Production-frame regressions prove dense bars perform no text layout, status galleys retain the same Arc, and unchanged draw slots retain the same waveform Arc. |
| Sticky decode pool; cancel/coalesce; instant first play | Proven | `nle-decode` scheduler/session tests; no full-index gate. |
| Bounded live frame cache | Proven | CLI cap 512–2048 MiB; sparse-anchor and sustained-scrub hard-cap tests. |
| Analysis only after placement | Proven | 200-import test and `AnalyzeMedia` placement action. |
| Native `wgpu`; batched timeline; one monitor texture | Proven | `nle-render` instance buffers and callback tests; app owns one retained monitor texture. |
| Immediate UI; hold tool; command palette | Proven | `nle-ui-core` input and command tests. |
| Independent CPAL audio and cached peaks | Proven structurally | `nle-audio`, `nle-waveform`, native waveform primitive tests. |
| Probe/inspector/backend identity and software override | Proven on Windows | Every-stream metadata tests; actual software/Quick Sync/CUVID/D3D11VA/DXVA2 backend identity; every hardware backend retains a software runtime fallback; force-software resets the live session/cache. |
| Third-party licenses | Proven | In-app Licenses screen and packaged notices. |
| LGPL shared FFmpeg; no prohibited codec DLL | Proven for Windows package | Pinned n8.1 shared build reports LGPL 2.1+, package verification rejects GPL/nonfree/x264/x265/FDK AAC, and the adjacent-DLL-only smoke analyzes, decodes, plays, meters, and cancels export for a generated A/V clip. |
| macOS packaging and runtime | Out of scope | Historical build/package/workflow files are retained only as references. macOS is unsupported and is not a completion gate. |
| Reproducible project-built release FFmpeg | Proven on Windows | `build-ffmpeg-lgpl-windows.ps1` builds FFmpeg `9047fa1b...`, nv-codec-headers `1889e62e...`, and oneVPL `2274efcd...` under WSL, emits MSVC import libraries, records an artifact hash inventory, and enables NVDEC/NVENC, Quick Sync, D3D11VA/DXVA2, and Media Foundation. Forced in-process tests decode the supplied media through D3D11VA and DXVA2, including opaque-surface transfer; the normal preference path also proves hardware backward seeking. Packaging verifies all three revisions and all 42 hashes before building. Workspace media tests, real reverse-scrub gates, export, adjacent-DLL startup, real-media acceptance, and the 120-frame surface-submission gate pass against this bundle. |

## Current verification commands

```powershell
.\scripts\build-ffmpeg-lgpl-windows.ps1
$env:FFMPEG_DIR=(Resolve-Path '.deps\ffmpeg-project-8.1').Path
$env:LIBCLANG_PATH='C:\CraftRoot\bin'
$env:PATH="$env:FFMPEG_DIR\bin;C:\CraftRoot\bin;$env:PATH"
$env:MAELSTROM_TEST_MEDIA='C:\path\to\representative-video.mp4'
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p nle-ui-core --release --test timeline_performance -- --ignored --nocapture
cargo test -p nle-timeline --release release_fifty_thousand_clip_move_and_trim_finish_under_two_ms -- --nocapture
cargo test -p nle-timeline --release fifty_thousand_visible_range_is_sub_millisecond -- --nocapture
cargo test -p nle-render --release gpu_executes_banded_timeline_frames_under_eight_ms_p95 -- --ignored --nocapture
cargo test -p nle-audio supplied_media_device_clock_stays_within_one_24fps_frame -- --nocapture
cargo test -p nle-app forced_process_termination_recovers_latest_background_autosave -- --nocapture
.\scripts\package-windows.ps1 -FfmpegBundleRoot .\.deps\ffmpeg-project-8.1

```

Windows release-binary provenance is closed with a project-owned build and package proof. The
active foundation and interactive polish target is Windows. Linux remains an optional later
milestone; macOS is unsupported and out of scope.

## Final Windows verification

Completed on 2026-08-24 against commit `ca75658` plus this audit update:

| Gate | Result |
|---|---|
| `cargo test --workspace` | Pass; all default workspace tests green. The two release evidence tests intentionally ignored in debug were run separately and passed. |
| Strict workspace Clippy | Pass with `-D warnings`. |
| 50k retained editor canvas | Pass: wide p95 0.305 ms, detail p95 0.115 ms, playhead p95 0.123 ms. |
| Real H.264 plus 20,002 bars | Pass: input-to-visual p95 0.224 ms, 1,343 native primitives, latest 3 s frame delivered. |
| RTX 3090 native shader execution | Pass: 0.198 ms p95 for 1,000 banded instances. |
| Fresh Windows package | Pass: first present 608.705 ms; 158.13 submissions/s; interval p95 6.949 ms; CPU p95 1.149 ms. |
| Packaged real-media acceptance | Pass: 875.82 px timeline over 444.18 px viewer, fitted 60 s source, linked V/A bars, 2,048 waveform peaks, 1.462 s playback, live meters, export progress, clean cancellation. |
| Clickable test binary | `test-build\\Maelstrom-Test.exe`, SHA-256 `37AEF672F293B9B246EE7B2FAE793DEF02532F0C8F5B28215A28746C458DB264`. |
| Process cleanup | Pass: no Maelstrom, `nle-app`, FFmpeg, or FFprobe process remained after verification. |

Every Windows foundation requirement in the v1.1 guide and every hard user-directed regression in
scope has direct source, automated, packaged, or hands-on evidence above. No required Windows
foundation item remains open. Product model artifacts, light mode, Linux packaging, effects,
multiple timelines, and AI features are later product work rather than incomplete foundation work.
